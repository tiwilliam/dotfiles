[user]
	name = AUTHORNAME
	email = AUTHOREMAIL

[alias]
    co = checkout
    up = !git pull --rebase
    count = !git shortlog -sn

[color]
    diff = auto
    status = auto
    branch = auto

[core]
    excludesfile = ~/.gitignore
    editor = vim
